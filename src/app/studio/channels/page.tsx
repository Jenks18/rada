import { ChannelManager } from '@/components/studio/ChannelManager';

export default function ChannelsPage() {
  return (
    <div className="container mx-auto px-4 py-8">
      <ChannelManager />
    </div>
  );
}
